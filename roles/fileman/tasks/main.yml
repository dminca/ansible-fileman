---
# debugging task
# - debug: var=playbook_dir

# grab the folder names
- name: Store folder names in var
  command: ls /tmp/testing
  register: test_folders

# register a directories var
- name: Getting the stats of directories in specific folder
  stat: path="/tmp/testing/{{ item }}"
  with_items: "{{ test_folders.stdout_lines }}"
  register: st

- debug: var=st


# print the directories stats


# move directories older than 6 months+ in another folder


# symlink the moved directories back to the previous folder